import React from 'react';

const ShareSectionColumn = (props) => {
	return (
		<div className="h-100 w-50 flex flex-column items-end">
			{props.children}
			
		</div>
		)
}

export default ShareSectionColumn; 